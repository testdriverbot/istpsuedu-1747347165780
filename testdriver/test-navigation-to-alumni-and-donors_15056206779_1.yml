version: 4.2.29
session: 68266a6032878913ca8e2e10
steps:
  - prompt: Click on the "Alumni and Donors" link in the navigation menu.
    commands:
      - command: hover-text
        text: Alumni and Donors
        description: link in the navigation menu
        action: click
  - prompt: Assert that the Alumni and Donors page is displayed.
    commands:
      - command: assert
        expect: the page contains "Alumni & Donors"
  - prompt: Assert that there are no errors on the page.
    commands:
      - command: assert
        expect: the page does not contain "Error" or "404"
